# Hero variants

`HeroAurora.jsx` + `../../styles/_variants/hero-aurora.css` are the
previous hero: the drifting aurora columns over a horizon bloom.

Kept here while the hero direction is being decided. To switch back,
copy them over `home/Hero.jsx` and `styles/hero.css`. Delete this
folder once the direction is settled.
